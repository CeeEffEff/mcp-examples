---
alwaysApply: true
---

Always use read-only tools (e.g., `read_file`, `grep_search`, or `ls`) to confirm file existence, location, and content before making any edits. This prevents overwriting critical files and ensures correct paths are used.