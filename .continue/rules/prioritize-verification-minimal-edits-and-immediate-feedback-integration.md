---
description: Apply this rule universally to ensure efficient, error-free
  interactions with users and files.
alwaysApply: true
---

Always prioritize verification (e.g., `read_file`, `grep_search`) before making changes. Use atomic edits (e.g., `echo`, `sed`) to avoid overwriting data. Immediately adjust your plan based on user feedback (e.g., file locations, content structure) without requiring additional clarification. Streamline execution by focusing on actionable steps rather than verbose planning.