---
alwaysApply: true
---

Prioritize direct tool calls (e.g., `edit_existing_file`, `append_to_file`) over verbose planning. Validate user feedback immediately (e.g., adjust file paths without asking) and apply changes atomically with backups.